# 项目记忆总览

本文件用于防止上下文丢失。每次继续工作前先读本文件，再读 `AGENTS.md`、`workflow.md`、`env.md` 和 `logs/issue_log.md`。

## 项目目标

- 构建 Unity3D 越野仿真环境。
- 在仿真中实现 VLN 感知层两个核心输入：相机图像和 3D LiDAR 点云。
- 通过 ROS2 Humble 输出标准 topic，供后续感知、建图、VLM/VLN 推理、规划模块使用。

## 当前用户环境事实

- 操作系统：Ubuntu 22.04.5 LTS。
- CPU：AMD Ryzen 9 8945HX，16 核 32 线程。
- GPU：NVIDIA GeForce RTX 5060 Laptop GPU，显存约 8GB。
- NVIDIA 驱动：580.173.02。
- `nvidia-smi` 显示 CUDA Runtime 支持：13.0。
- 系统 `nvcc`：CUDA 13.0.48，`/usr/local/cuda -> /usr/local/cuda-13.0/`。
- Python：`/usr/bin/python3`，Python 3.10.12。
- PyTorch：2.10.0+cu128，`torch.cuda.is_available()` 为 True，PyTorch CUDA 为 12.8。
- 内存：约 30GiB，总体可用约 19GiB。
- 磁盘：根分区约 1.5T，可用约 1.2T。
- ROS2：已有 Humble，且已有 `rviz2`、`rqt`、`navigation2/nav2_bringup`、`sensor_msgs`、`tf2_ros`、`image_transport`。
- 用户自定义函数：`ros2env`，用于清理 Conda 相关环境并加载 ROS2 Humble。
- 代理：本机 Clash/Mihomo 监听 `127.0.0.1:7897`，环境变量已有 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY`。
- Unity Hub：项目内用户级安装，应用菜单入口为 `Unity Hub (VLN)`，启动脚本为 `/home/ubuntu22/VLN/scripts/run_unityhub.sh`。
- Unity Editor：`/home/ubuntu22/VLN/UnityEditors/2022.3.62f1/Editor/Unity`，版本 `2022.3.62f1`，Unity Personal 许可证已激活并通过空工程创建探测。
- Unity 正式工程：`/home/ubuntu22/VLN/UnityProjects/VLN_Offroad`，已导入 `com.unity.robotics.ros-tcp-connector`、`com.frj.unity-sensors`、`com.frj.unity-sensors-ros`、`com.unity.ugui`、`com.unity.test-framework`、`com.unity.robotics.urdf-importer`。
- Unity-ROS2 最小通信闭环：已通过。Unity 发布 `/unity/heartbeat`，ROS2 echo 成功；ROS2 发布 `/ros2/command`，Unity 接收成功。
- UnitySensors 相机图像闭环：已通过。Unity 发布 `/vln/front/image_raw`，ROS2 校验为 `sensor_msgs/msg/Image`、640x480、`rgb8`、`front_camera_optical_frame`、约 5Hz；同时发布 `/vln/front/camera_info`。
- UnitySensors LiDAR 点云闭环：已通过。UnitySensors VLP-16 Raycast LiDAR 发布 `/vln/lidar/points`，ROS2 校验为 `sensor_msgs/msg/PointCloud2`、`lidar_link`、7200 点/帧、`point_step=16`、约 5Hz、带宽约 0.6 MB/s。
- 极简越野 terrain 闭环：已通过。新增场景 `Assets/VLN/Scenes/VLNOffroadTerrainSmokeTest.unity`，使用轻量程序化网格地形、土路、坡、石块、树木、障碍物和静态占位车体传感器 rig；同一场景内同时发布 `/vln/front/image_raw`、`/vln/front/camera_info` 和 `/vln/lidar/points`，阶段 6 脚本输出 `VLN_OFFROAD_TERRAIN_SMOKE_TEST_PASS`。
- 可控占位车体与 TF 树闭环：已通过。`VlnVehicleTfPublisher` 发布 `/tf`，当前 TF 树为 `map -> base_link`、`base_link -> front_camera_optical_frame`、`base_link -> lidar_link`；Unity Play 后无 `/vln/cmd_vel` 时默认静止，传感器挂载在车体下，阶段 7 脚本输出 `VLN_VEHICLE_TF_SMOKE_TEST_PASS`。
- 标准化输出闭环：已通过。阶段 8 固定 topic、frame、RViz 配置和 rosbag 记录流程；`/home/ubuntu22/VLN/scripts/run_standardized_outputs_smoke_test.sh` 已输出 `VLN_STANDARDIZED_OUTPUTS_SMOKE_TEST_PASS`，并记录 8 秒标准 rosbag 样本；阶段 8 默认验收车体静止，不再依赖自动巡航。
- ROS2 控制闭环：已通过。Unity 订阅 `/vln/cmd_vel [geometry_msgs/msg/Twist]`，ROS2 发布速度指令后占位车体响应运动，传感器和 `/tf` 随车体更新；阶段 9 脚本输出 `VLN_CMD_VEL_CONTROL_SMOKE_TEST_PASS`。
- ROS2 路径点控制闭环：已通过。ROS2 侧控制器读取 `/tf` 中的 `map -> base_link`，计算并发布 `/vln/cmd_vel`，驱动 Unity 占位车体到达相对路径点；阶段 10 脚本输出 `VLN_WAYPOINT_CONTROL_SMOKE_TEST_PASS`。
- 本地中文控制面板：已通过。浏览器 UI 提供“目标位置 / 相机视图 / 雷达点云”三个模块；目标位置模块输入相对坐标并由 ROS2 后端发布 `/vln/cmd_vel`；相机和雷达按钮触发已有 rqt/RViz 脚本。
- 成熟资产升级工作流：已建立。第一轮复杂越野地图候选已导入 Kenney Nature Kit 2.1 的轻量 FBX 子集，候选场景为 `Assets/VLN/Scenes/VLNOffroadAssetCandidate.unity`；主场景 `VLNOffroadTerrainSmokeTest.unity` 仍作为回归基线，不被覆盖。
- 真实小车视觉候选：已下载 Clearpath Husky 与 Jackal 源仓到 `/home/ubuntu22/VLN/VLN_ASSETS_CACHE/vehicles/`。第一轮导入 Husky 视觉 mesh 子集，候选场景为 `Assets/VLN/Scenes/VLNOffroadVehicleCandidate.unity`；Jackal 已保留为第二候选，暂不导入。注意：Husky ROS description mesh 是低多边形工程模型，不是高清游戏级车模；Game 视图如果 Scale 设成 `10x` 会把画面放大成马赛克。
- URDF/STL 物理车体阶段：已按师兄提供的 `scout_v2.xacro` 确定 AgileX Scout V2 作为阶段 13 第一候选。已通过代理轻量下载 `agilexrobotics/ugv_gazebo_sim` 的 `scout/scout_description` 子目录到 `/home/ubuntu22/VLN/VLN_ASSETS_CACHE/vehicles/ugv_gazebo_sim_scout_description_raw`，commit `27633a956c845903ee630538afeb17fe70afdd84`；共 40 个文件，约 94.8MB。`scout_v2.xacro` 已在缓存 staging 中成功展开为 `generated/scout_v2.urdf`，包含 Scout V2 底盘、四个连续轮关节、6 个 collision 和 5 个 inertial。阶段 13 导入前链路冻结已通过，run id 为 `vln_asset_baseline_20260815_182915`；阶段 13 导入后完整资产基线回归也已通过，最新 run id 为 `vln_asset_baseline_20260815_191337`。阶段 13 工作流文档为 `/home/ubuntu22/VLN/docs/urdf_physics_vehicle_workflow.md`。
- Scout V2 Unity URDF 候选：已加入 Unity 工程级 URDF Importer，生成独立场景 `Assets/VLN/Scenes/VLNOffroadScoutUrdfCandidate.unity`，不覆盖旧地图/车体候选。Unity 导入入口为 `Assets/VLN/ExternalAssets/ScoutUrdfPhysics/scout_v2_unity_import.urdf`，mesh 子集为 `meshes/base_link.dae` 与 `meshes/wheel_type1.dae`；URDF Importer 自动生成 `Materials/Default.mat` 与 `meshes/Cylinder.asset` 用于默认材质和 cylinder collision。关键姿态设置为 `ImportSettings.axisType.yAxis`；第一次用 `zAxis` 会让车体竖起来，`yAxis` 后尺寸约 `0.700 x 0.351 x 0.930m`，车身平放、四轮竖直贴地。Scout 候选已新增 `/vln/odom [nav_msgs/msg/Odometry]`，由 `VlnOdomPublisher` 按 Unity rig 实际位姿差分发布，frame 为 `map`，child frame 为 `base_link`。最新静态验收 run id `vln_scout_urdf_candidate_20260815_192630` 通过；最新控制验收 run id `vln_scout_urdf_cmd_vel_20260815_195941` 通过，`odom_delta=2.260m`、`odom_yaw_delta=2.836rad`、`wheel_found_count=4`、`wheel_command_count=48`、`nonzero_target_count=4`。阶段 13 当前结论是“URDF 物理结构 + ROS2 控制信号已接到 wheel joint drive + `/vln/odom` 候选输出 + 现有运动学 rig 仍负责整车位移”，还不是完整轮胎-地面摩擦、悬挂、电机和 `/joint_states` 动力学。
- Scout wheel-ground 真实动力学候选：已新增独立场景 `Assets/VLN/Scenes/VLNOffroadScoutWheelGroundCandidate.unity`，不覆盖旧 Scout URDF 候选。该场景使用 `ScoutWheelGround_PhysicsRoot` 作为真实物理根，包含 `Rigidbody`、chassis `BoxCollider` 和四个 `WheelCollider`；`ScoutWheelGround_VisualUrdf` 复用 Scout V2 URDF mesh 但剥离 collider、Rigidbody、ArticulationBody 和 URDF 脚本，只作为视觉模型。`VlnScoutWheelGroundController` 订阅 `/vln/cmd_vel` 并通过轮地接触驱动小车，`VlnVehicleTfPublisher` 在该场景关闭 `m_EnableKinematicMotion`，`VlnFollowTransformPose` 让相机/LiDAR rig 跟随物理根，`/vln/odom` 保持输出。最新验收 run id `vln_scout_wheel_ground_20260815_195417` 通过，`physics_root_delta_m=1.7739`、`forward_delta=1.771m`、`odom_forward_delta=1.771m`、`wheel_collider_count=4`、`visual_collider_count=0`、`visual_articulation_body_count=0`、`motor_command_count=614`。当前是第一版可验证 wheel-ground 候选，不等于最终论文级车辆动力学标定；后续还需验证差速转向、坡地、碰撞、摩擦、质量/惯性和 `/joint_states`。
- Scout wheel-ground 固定路线物理巡航：已升级为阶段 15 完整路线演示，不再以 9m 短路线作为默认完成标准。默认路线为相对 `base_link` 前向 `4,0;8,0;12,0;15,0;18,0;22,0;28,0;34,0;42,0;50,0;54,0`，用于从起点沿道路通过桥/坡区域并跑向终点方向。修复项包括：移除 wheel-ground 视觉 URDF 的额外 `180°` yaw，避免肉眼看成倒着开；把轮胎视觉偏移提高到 `0.085m`，缓解轮胎看起来陷地；为桥面新增不可见物理过渡坡，解决桥面硬边让 WheelCollider 卡住的问题；默认巡航速度提高到 `max_linear=1.35m/s`、`linear_accel=0.95m/s^2`，并保留 `--skip-stalled-waypoints` 作为物理演示容错。最新完整路线通过 run id `vln_scout_wheel_ground_route_20260816_150349`，结果为 `reached_count=11/11`、`total_forward_progress=53.080m`、`total_progress=53.102m`、`stall_count=0`、`skipped_count=0`，同时图像、CameraInfo、LiDAR 点云和 `/vln/odom` 通过。随后基础 wheel-ground 回归 `vln_scout_wheel_ground_20260816_150809` 通过，直行 5 秒前向位移约 `3.466m`，截图显示车体姿态正常、轮胎不再明显陷地。

## 已知风险

- RTX 50 系列对 CUDA / PyTorch 版本匹配敏感，禁止擅自改动相关环境。
- ROS2 与 Conda 易冲突，ROS2 命令应通过 `ros2env` 进入干净环境。
- 当前 shell 已经存在 ROS2/PX4 相关 `PYTHONPATH`、`LD_LIBRARY_PATH`，后续验证时不要假设这是理想干净环境。
- Unity 资产、ROS2 build/install/log、rosbag 和外部资料很容易造成 git 爆仓，必须依赖 `.gitignore` 和仓库外资料库。
- 当前目录 `/home/ubuntu22/VLN` 暂未初始化为 git 仓库；`.gitignore` 仍先建立，防止后续初始化后误提交垃圾文件。
- 运行环境中曾出现敏感 token 环境变量；后续日志和文档禁止记录任何 token、密钥、账号凭据。
- Unity URDF Importer / Assimp 在日志里可能出现一次 `DllNotFoundException: libdl.so`。当前用 Editor 导入路径规避，静态、控制和基线回归均已通过；不要为了该日志擅自安装系统库或改 Unity Editor。
- ROS2/rclpy 日志统一写到 `/home/ubuntu22/VLN/.ros/log`。后续新增 ROS2 shell 或 Python 验收脚本时必须显式继承 `ROS_LOG_DIR`，避免写入 `/home/ubuntu22/.ros/log`。

## 当前结论

- 这台机器可以做 Unity + ROS2 + 中等规模越野仿真。
- 8GB 显存是主要边界：初期建议 Built-in/URP、低中等地形复杂度、单相机或低分辨率全景相机、VLP-16/Mid360 级 LiDAR、5-10Hz 传感器频率。
- 暂不建议一开始上 HDRP、大面积高密度植被、VLS-128 高频点云、多路高分辨率相机或端到端大模型在线训练。

## 最近一次工作记录

- 2026-08-13：完成本机硬件/环境只读体检；建立项目长期约束、环境文档、工作流、问题日志和 `.gitignore` 初始版本。
- 2026-08-13：按 Unity-ROS2 主路线推进阶段 3 的 ROS2 侧准备；在 `/home/ubuntu22/VLN/unity_ros2_ws` 克隆 ROS-TCP-Endpoint `main-ros2` 分支；依赖检查通过；普通 `colcon build --packages-select ros_tcp_endpoint` 成功；短暂启动 endpoint 后确认 `127.0.0.1:10000` 可监听。未安装任何新包。
- 2026-08-13：发现 endpoint 停止时重复 `rclpy.shutdown()` 产生 traceback；对本地 ROS-TCP-Endpoint 做最小补丁并重构建；验证启动脚本可监听 `127.0.0.1:10000`，停止后端口释放且无残留进程。
- 2026-08-13：Unity Hub 已项目内解包到 `/home/ubuntu22/VLN/tools/unityhub_extracted_3.20.1`；Unity Editor `2022.3.62f1` 已项目内安装到 `/home/ubuntu22/VLN/UnityEditors/2022.3.62f1/Editor/Unity` 并通过版本验证；已创建用户级应用菜单入口 `Unity Hub (VLN)`。
- 2026-08-13：Unity Hub 用户级菜单入口已补充 `unityhub://` 回调协议；`scripts/run_unityhub.sh` 已把项目内 Unity Hub bin 目录加入 PATH，避免 Hub 内部找不到自身可执行文件。
- 2026-08-13：Unity Hub 登录卡安全检查已定位：浏览器曾报 `Conversation Ip Violation`，Hub 日志显示 OAuth 回调后 POST `https://api.unity.com/v1/oauth2/token` 发生 `ECONNRESET`；当前判断为 Unity 登录链路的代理出口/IP 一致性问题，而非账号注册本身问题。后续登录前固定同一节点，优先用 Clash/Mihomo 全局/TUN，浏览器与终端 `api.ipify.org` 出口必须一致。
- 2026-08-13：二次登录截图确认 `conversationIp` 为直连 IP、`userIp` 为代理 IP；已加固 Unity Hub 启动脚本并新增登录网络检查脚本。结论：系统级安装不是当前根因；必须丢弃旧登录 conversation，重启 Hub 后再试。
- 2026-08-13：Unity 登录进一步卡在短信 2FA；用户点击重新发送短信后手机不再收到新码。结论：不要继续频繁重发；如果能进 Unity ID 网页则在 Security 中移除 2FA；如果进不去则使用恢复码或提交 Unity Support 工单。项目主线可转为直接尝试本地 Unity Editor 创建工程。
- 2026-08-13：本地 Unity Editor 许可证探测完成：`-version` 可输出 `2022.3.62f1`，但 `-createProject` 空工程失败，日志显示 `No valid Unity Editor license found. Please activate your license.` 因此“完全无账号/无许可证”不能完整推进 Unity 主线；账号问题可短期绕开做 ROS2、资料、工程配置准备，但进入 Editor、导入包、Play 模式和传感器闭环需要有效 Unity 许可证。
- 2026-08-13：定位 `Launching Unity Hub` 后仍回登录页的问题：用户级 `.desktop` 协议处理器缺少 `%u`，导致浏览器 `unityhub://` OAuth 回调参数被丢弃。已改为 `Exec=/home/ubuntu22/VLN/scripts/run_unityhub.sh %u` 并重新注册 MIME。
- 2026-08-13：用户已成功登录 Unity Hub；Hub 日志显示 Unity Personal 许可证已激活，许可证文件位于 `/home/ubuntu22/VLN/.unity_user/config/unity3d/Unity/licenses/UnityEntitlementLicense.xml`。重新执行 Unity Editor `-createProject` 探测成功，退出码 `0`，已创建 `/home/ubuntu22/VLN/UnityProjects/_LicenseProbe_20260813_215209`。后续不要点击 Hub 推荐的 Unity 6.5 安装，主线继续使用已安装的 `2022.3.62f1`。
- 2026-08-13：正式 Unity 工程 `/home/ubuntu22/VLN/UnityProjects/VLN_Offroad` 已创建成功；`Packages/manifest.json` 已加入 `com.unity.robotics.ros-tcp-connector`；Unity 批处理导入退出码 `0`，`Packages/packages-lock.json` 锁定 Connector 到 Git hash `c27f00c6cf750d2d0564349b3039d19aa3925e7c`。新增固定打开脚本 `/home/ubuntu22/VLN/scripts/open_unity_vln_project.sh`，该脚本复用项目内 Unity 配置、许可证和代理。
- 2026-08-13：阶段 3 Unity-ROS2 最小通信闭环已通过。新增 `Assets/VLN/Scripts/VlnRos2SmokeTest.cs`、`Assets/VLN/Editor/VlnRos2ProjectSetup.cs`、`Assets/VLN/Editor/VlnRos2SmokeTestRunner.cs` 和 `/home/ubuntu22/VLN/scripts/run_ros2_unity_smoke_test.sh`；Unity 工程设置 `ROS2` 编译符号；复现命令输出 `VLN_ROS2_SMOKE_TEST_PASS`，最近通过日志目录为 `/home/ubuntu22/VLN/UnityProjects/_SmokeTestLogs/vln_smoke_20260813_221253`。
- 2026-08-13：阶段 4 UnitySensors 相机图像闭环已通过。`Packages/manifest.json` 加入 UnitySensors / UnitySensorsROS / UGUI / Test Framework；新增 `Assets/VLN/Scenes/UnitySensorsImageSmokeTest.unity`、相机场景构建器、运行时退出脚本、ROS2 图像字段校验脚本和 `/home/ubuntu22/VLN/scripts/run_unitysensors_image_smoke_test.sh`；复现命令输出 `VLN_UNITYSENSORS_IMAGE_SMOKE_TEST_PASS`，最近通过日志目录为 `/home/ubuntu22/VLN/UnityProjects/_SmokeTestLogs/vln_image_20260813_224841`。
- 2026-08-13：阶段 4 完成后回归阶段 3 通信闭环，`/home/ubuntu22/VLN/scripts/run_ros2_unity_smoke_test.sh` 再次输出 `VLN_ROS2_SMOKE_TEST_PASS`，run id 为 `vln_smoke_20260813_224611`。
- 2026-08-13：阶段 5 UnitySensors LiDAR 点云闭环已通过。新增 `Assets/VLN/Scenes/UnitySensorsLidarSmokeTest.unity`、LiDAR 场景构建器、运行时退出脚本、ROS2 点云字段校验脚本和 `/home/ubuntu22/VLN/scripts/run_unitysensors_lidar_smoke_test.sh`；最近复现命令输出 `VLN_UNITYSENSORS_LIDAR_SMOKE_TEST_PASS`，run id 为 `vln_lidar_20260813_230736`。点云规格为 `/vln/lidar/points`、`sensor_msgs/msg/PointCloud2`、`lidar_link`、VLP-16、7200 点/帧、约 5Hz。
- 2026-08-13：阶段 5 完成后回归阶段 4 相机闭环，`/home/ubuntu22/VLN/scripts/run_unitysensors_image_smoke_test.sh` 再次输出 `VLN_UNITYSENSORS_IMAGE_SMOKE_TEST_PASS`，run id 为 `vln_image_20260813_230503`。下一阶段只做极简越野 terrain 闭环，不导入大型资产包。
- 2026-08-13：修复手工可视化问题。`rqt_image_view` 不能作为独立命令时改用 `/home/ubuntu22/VLN/scripts/view_front_image.sh`；所有用户脚本移除 `rg` 依赖，改用系统自带 `grep`；RViz 点云查看脚本 `/home/ubuntu22/VLN/scripts/view_lidar_rviz.sh` 会临时发布 `map -> lidar_link` 静态 TF，并使用 `Fixed Frame=map`；ImageTest 场景构建器已补 `ImageSmokeTest_ViewerCamera`，关闭 Unity 后运行 `/home/ubuntu22/VLN/scripts/rebuild_unity_smoke_scenes.sh` 可重建场景让 Game 面板显示普通相机画面。
- 2026-08-14：处理 Unity Editor 卡死。已强制结束 VLN 工程相关 Unity Editor/worker 进程，并移动残留 `ArtifactDB-lock`、`SourceAssetDB-lock` 到 `_ManualRecoveryLogs`；新增 `/home/ubuntu22/VLN/scripts/stop_unity_vln_project.sh` 用于后续一键恢复。随后重建 smoke 场景成功，点云回归通过 `vln_lidar_20260813_235800`，图像回归通过 `vln_image_20260813_235944`。注意：Unity smoke test 不允许并行跑，只能顺序运行。
- 2026-08-14：继续修复手工 LiDAR 可视化排障。用户截图中 RViz `PointCloud2` 显示项为 OK 但画面只有网格；本地只读订阅 `/vln/lidar/points` 时未收到实时点云帧，说明“topic 存在/显示项 OK”不等价于“Unity 正在发布点云”。已增强 `/home/ubuntu22/VLN/scripts/check_manual_visualization_state.sh`，增加实时等待一帧 PointCloud2 的检查；同时将 `/home/ubuntu22/VLN/config/vln_lidar_pointcloud.rviz` 的 PointCloud2 显示改为橙色大点、`Best Effort` 订阅，以降低 RViz 显示误判。
- 2026-08-14：阶段 6 极简越野 terrain 闭环已通过。新增 `Assets/VLN/Editor/VlnOffroadTerrainProjectSetup.cs`、`Assets/VLN/Editor/VlnOffroadTerrainSmokeTestRunner.cs`、`Assets/VLN/Scripts/VlnOffroadTerrainSmokeTest.cs` 和 `/home/ubuntu22/VLN/scripts/run_offroad_terrain_smoke_test.sh`；最终通过 run id `vln_offroad_20260814_004120`，图像约 5Hz，点云约 5Hz，点云有效非零点 `3316`，带宽约 `0.58 MB/s`。随后顺序回归阶段 4/5：图像 `vln_image_20260814_004216` 通过，LiDAR `vln_lidar_20260814_004250` 通过。注意：阶段 6 联合相机/terrain 场景不能用 `-nographics`，需使用 `-batchmode` 保留图形上下文，否则 Unity 会在 `RGBCameraSensor` 的 `Camera.Render` 路径段错误。
- 2026-08-14：阶段 7 可控占位车体 + TF 树闭环已通过。新增 `Assets/VLN/Scripts/VlnVehicleTfPublisher.cs`、`/home/ubuntu22/VLN/scripts/ros2_wait_for_vehicle_tf.py`、`/home/ubuntu22/VLN/scripts/ros2_wait_for_camera_info_once.py` 和 `/home/ubuntu22/VLN/scripts/run_vehicle_tf_smoke_test.sh`；当前规则为 Unity Play 后默认静止，运动必须来自 ROS2 `/vln/cmd_vel`。最近静止回归 run id `vln_vehicle_tf_20260814_021645`，TF 树为 `map -> base_link -> front_camera_optical_frame,lidar_link`，无指令窗口内 `max_base_delta=0.000m`。
- 2026-08-14：阶段 8 标准化输出闭环已通过。新增 `/home/ubuntu22/VLN/scripts/run_standardized_outputs_smoke_test.sh`、`check_standardized_vln_outputs.sh`、`record_vln_sensor_bag_sample.sh`、`view_vln_vehicle_rviz.sh` 和 `/home/ubuntu22/VLN/config/vln_vehicle_sensors.rviz`；最近回归 run id `vln_standardized_outputs_20260814_021919` 输出 `VLN_STANDARDIZED_OUTPUTS_SMOKE_TEST_PASS`。本次 rosbag 位于 `/home/ubuntu22/VLN/VLN_BAGS/vln_standardized_outputs_20260814_021919`，包含 `/vln/front/image_raw` 40 帧、`/vln/front/camera_info` 39 帧、`/vln/lidar/points` 39 帧、`/tf` 78 条消息，大小约 `39.6 MiB`，标准输出阶段车体无指令静止。
- 2026-08-14：阶段 9 ROS2 `/vln/cmd_vel` 控制闭环已通过。扩展 `Assets/VLN/Scripts/VlnVehicleTfPublisher.cs`，新增 `/home/ubuntu22/VLN/scripts/ros2_send_cmd_vel_and_wait_tf.py` 和 `/home/ubuntu22/VLN/scripts/run_cmd_vel_control_smoke_test.sh`；最近自动验收 run id `vln_cmd_vel_control_20260814_021738` 输出 `VLN_CMD_VEL_CONTROL_SMOKE_TEST_PASS`。ROS2 发布 `linear.x=0.8`、`angular.z=0.7` 的 `geometry_msgs/msg/Twist`，Unity 收到 48 条指令，`base_link` 位移约 `2.262m`，yaw 变化约 `2.851rad`，同时图像、CameraInfo 和点云字段校验全部通过。
- 2026-08-14：阶段 10 ROS2 路径点控制闭环已通过。新增 `/home/ubuntu22/VLN/scripts/ros2_drive_waypoints.py` 和 `/home/ubuntu22/VLN/scripts/run_waypoint_control_smoke_test.sh`；最近自动验收 run id `vln_waypoint_control_20260814_021829` 输出 `VLN_WAYPOINT_CONTROL_SMOKE_TEST_PASS`。ROS2 侧根据 `/tf` 计算 `/vln/cmd_vel`，默认相对路径点为 `1.2,0.0;2.4,0.0`，到达 2/2 个路径点，总位移约 `2.100m`，最终距离最后路径点约 `0.300m`，同时图像、CameraInfo 和点云字段校验全部通过。
- 2026-08-14：修复用户反馈“点击 Play 后未发目标小车也会自动跑”。根因是 `m_AutopilotUntilFirstCommand=true`；已将脚本默认值、场景生成器和已保存主场景都改为 false，并把阶段 7/8 验收改成无指令静止。已回归静止、`/vln/cmd_vel`、路径点控制和标准输出四条主线。
- 2026-08-14：完成阶段 11 本地中文控制面板。新增 `/home/ubuntu22/VLN/scripts/vln_control_panel.py`、`start_vln_control_panel.sh`、`vln_control_panel_smoke_client.py` 和 `run_control_panel_smoke_test.sh`；UI 截图归档到 `/home/ubuntu22/VLN/UnityProjects/_SmokeTestLogs/control_panel_screenshots/vln_control_panel_ui_20260814_025613.png`；自动验收 run id `vln_control_panel_20260814_025329` 输出 `VLN_CONTROL_PANEL_SMOKE_TEST_PASS`，HTTP 发送相对目标 `1.20,0.00` 后 Unity 收到 30 条 `/vln/cmd_vel`，到达目标附近。
- 2026-08-14：建立阶段 12 成熟越野地图/真实小车模型升级工作流。新增 `/home/ubuntu22/VLN/docs/asset_upgrade_workflow.md` 和资料库索引 `/home/ubuntu22/VLN/VLN_REFERENCE_LIBRARY/asset_candidates/index.md`；当前只做候选来源和导入测试流程记录，未下载或导入大型资产。
- 2026-08-14：完成阶段 12 第一轮复杂越野地图候选导入。使用代理从 Kenney 下载 `kenney_nature-kit.zip` 到 `/home/ubuntu22/VLN/VLN_ASSETS_CACHE/`，SHA256 为 `fa7974a0d342bfe63c38664ba9f8ec1a4aab8ea25f099bdc56870e33588c4d9d`；只导入 70 个轻量 FBX 子集到 `Assets/VLN/ExternalAssets/KenneyNatureKit`，并生成候选场景 `Assets/VLN/Scenes/VLNOffroadAssetCandidate.unity`。候选场景保留现有占位车体、`/vln/cmd_vel`、相机、LiDAR 和 TF。验收脚本 `/home/ubuntu22/VLN/scripts/run_offroad_asset_candidate_smoke_test.sh` 输出 `VLN_OFFROAD_ASSET_CANDIDATE_SMOKE_TEST_PASS`；完整资产升级回归 `/home/ubuntu22/VLN/scripts/run_asset_upgrade_baseline_check.sh` 输出 `VLN_ASSET_UPGRADE_BASELINE_CHECK_PASS`，最新 run id 为 `vln_asset_baseline_20260814_033514`。
- 2026-08-14：完成阶段 12 第一轮真实小车候选评估和 Husky 视觉导入。使用本地代理 shallow clone `https://github.com/husky/husky.git` 的 `humble-devel` 分支到 `/home/ubuntu22/VLN/VLN_ASSETS_CACHE/vehicles/husky`，commit `729f8aa45ccd86fa33a05e07ef698c52c451cd9c`；shallow clone `https://github.com/jackal/jackal.git` 的 `foxy-devel` 分支到 `/home/ubuntu22/VLN/VLN_ASSETS_CACHE/vehicles/jackal`，commit `017b8b581a90873047f7d6fe438bd87513be4a76`。第一轮选择 Husky，因为它有 `.dae` 视觉 mesh 且有 Humble 分支；Jackal 主要是 `.stl`，先作为第二候选保留。Unity 只导入 Husky 5 个必要 `.dae` 到 `Assets/VLN/ExternalAssets/HuskyVisual`，约 `5.0M`，并生成候选场景 `Assets/VLN/Scenes/VLNOffroadVehicleCandidate.unity`。导入策略是“视觉车体替换”，保留现有 `/vln/cmd_vel`、`/tf`、相机和 LiDAR rig。验收脚本 `/home/ubuntu22/VLN/scripts/run_offroad_vehicle_candidate_smoke_test.sh` 输出 `VLN_OFFROAD_VEHICLE_CANDIDATE_SMOKE_TEST_PASS`；完整资产升级回归 `/home/ubuntu22/VLN/scripts/run_asset_upgrade_baseline_check.sh` 输出 `VLN_ASSET_UPGRADE_BASELINE_CHECK_PASS`，最新 run id 为 `vln_asset_baseline_20260814_040515`。
- 2026-08-14：修正用户反馈“Unity Game 视图里车体和场景过糊”。根因一是用户截图中 Game 视图 Scale 为 `10x`，会把渲染结果像素级放大；根因二是 Husky ROS description mesh 本身是工程低多边形模型，不是高清资产。已将 `VLNOffroadVehicleCandidate.unity` 的默认 Game 展示相机改为 `VehicleCandidate_GameCamera` 近距离视角，并仅在该候选场景把 `/vln/front/image_raw` 提高到 `1280x720`；验收脚本 `/home/ubuntu22/VLN/scripts/run_offroad_vehicle_candidate_smoke_test.sh` 已通过，run id `vln_offroad_vehicle_candidate_20260814_095155`，CameraInfo 同步为 `1280x720`。
- 2026-08-14：修正 Husky 视觉候选“四脚朝天/露底盘”的姿态错误。根因是 Unity 导入 Z-up DAE 后可见 mesh 仍需要额外 upright correction；已在 `VlnOffroadVehicleCandidateProjectSetup.cs` 中对每个 Husky mesh 实例追加 `Quaternion.AngleAxis(180f, Vector3.right)`，只影响视觉模型，不改 `/tf`、`/vln/cmd_vel`、相机或 LiDAR rig。验收脚本 `/home/ubuntu22/VLN/scripts/run_offroad_vehicle_candidate_smoke_test.sh` 已通过，run id `vln_offroad_vehicle_candidate_20260814_101556`；近景截图显示黄色上盖在上、四个轮子竖直贴地。
- 2026-08-14：Husky 姿态修复后完成完整资产升级回归，`/home/ubuntu22/VLN/scripts/run_asset_upgrade_baseline_check.sh` 输出 `VLN_ASSET_UPGRADE_BASELINE_CHECK_PASS`，run id `vln_asset_baseline_20260814_101820`。五项均通过：地图候选、车体候选、标准输出、`/vln/cmd_vel` 控制、中文控制面板；回归中的车体近景 run id 为 `vln_offroad_vehicle_candidate_20260814_101904`。
- 2026-08-14：修复用户反馈“Game/全局地图里不能随便拖动视角”。根因是 `Game` 标签页显示的是固定展示相机，不是 Unity `Scene` 标签页的编辑器自由视角。新增 `Assets/VLN/Scripts/VlnRuntimeMapCameraController.cs`，并在 terrain/vehicle 候选场景生成器中挂载到 `Offroad_ViewerCamera` 和 `VehicleCandidate_GameCamera`；同时加了运行时自动挂载逻辑，当前已打开的场景点击 Play 后也能自动获得控制。操作：左/右键拖动旋转，中键拖动平移，滚轮缩放，右键按住时 WASD/QE 移动。由于用户当前开着 Unity Editor，本轮未强制关闭窗口跑 batch 验收；已检查当前 Unity 日志未见 C# 编译错误。
- 2026-08-15：根据师兄指定的 `scout_v2.xacro` 开启阶段 13 URDF/STL 物理车体路线。完整 `git clone` 过慢且超时，因此通过代理只下载 `scout/scout_description` 子目录；原始半截克隆移动到 `VLN_ASSETS_CACHE/vehicles/ugv_gazebo_sim.partial_*` 保留。已完成 Scout V2 xacro 体检和 URDF 展开：`base_link` 视觉 mesh 为 `base_link.dae`，四个轮子使用 `wheel_type1.dae`，底盘 collision 为两个 box，轮子 collision 为 cylinder，质量参数为底盘/惯性 40kg、单轮 3kg。阶段 13 现有链路冻结已通过：`/home/ubuntu22/VLN/scripts/run_asset_upgrade_baseline_check.sh` 输出 `VLN_ASSET_UPGRADE_BASELINE_CHECK_PASS`，run id `vln_asset_baseline_20260815_182915`。下一步进入 Unity URDF Importer 准备和候选场景导入，必须保持旧 ROS2 图像、点云、TF 和 `/vln/cmd_vel` 接口不变。
- 2026-08-15：完成 Scout V2 URDF 候选导入第一轮。Unity 工程加入 `com.unity.robotics.urdf-importer` `v0.5.2`，锁定 hash `90f353e4352aae4df52fa2c05e49b804631d2a63`，并将 UPM 缓存固定到 `/home/ubuntu22/VLN/.unity_user/cache/upm`，避免访问 `/home/ubuntu22/.config/unity3d/cache`。新增 `VLNOffroadScoutUrdfCandidate.unity`、`VlnOffroadScoutUrdfCandidateProjectSetup.cs`、`VlnOffroadScoutUrdfCandidateSmokeTest.cs`、`run_scout_urdf_candidate_smoke_test.sh` 和 `run_scout_urdf_cmd_vel_smoke_test.sh`。最终静态验收通过 run id `vln_scout_urdf_candidate_20260815_185336`：6 links、5 joints、4 continuous wheel joints、5 inertials、6 URDF collisions、6 Unity colliders、17 renderers、5 articulation bodies、静止位移 `0.0000m`；控制验收通过 run id `vln_scout_urdf_cmd_vel_20260815_185425`：ROS2 发布 `/vln/cmd_vel` 后 base_link 位移 `2.257m`、yaw 变化 `2.821rad`，图像、CameraInfo、PointCloud2 和 TF 全部正常；当前最新完整资产基线回归 run id `vln_asset_baseline_20260815_191337` 也已通过。
- 2026-08-15：补上 Scout wheel joint 控制信号探针。新增 `Assets/VLN/Scripts/VlnScoutWheelJointCommandProbe.cs`，在 Scout 候选场景内把 `/vln/cmd_vel` 映射为四个 wheel ArticulationBody 的 `xDrive.targetVelocity`，但整车位移仍由现有 `VlnVehicleTfPublisher` 运动学 rig 承担。`run_scout_urdf_cmd_vel_smoke_test.sh` 已扩展检查 `wheel_found_count=4`、`wheel_command_count=48`、`nonzero_target_count=4`，run id `vln_scout_urdf_cmd_vel_20260815_191235` 通过；随后旧资产完整回归 `vln_asset_baseline_20260815_191337` 通过。
- 2026-08-15：完成 Scout 候选 `/vln/odom` 小步增强。新增 `Assets/VLN/Scripts/VlnOdomPublisher.cs` 和 `/home/ubuntu22/VLN/scripts/ros2_wait_for_odom_once.py`，并扩展 `ros2_send_cmd_vel_and_wait_tf.py`、`run_scout_urdf_candidate_smoke_test.sh`、`run_scout_urdf_cmd_vel_smoke_test.sh`；静态验收 `vln_scout_urdf_candidate_20260815_192630` 通过，控制验收 `vln_scout_urdf_cmd_vel_20260815_192732` 通过，`/vln/odom [nav_msgs/msg/Odometry]` 与 TF 运动一致。随后完整资产基线回归 `vln_asset_baseline_20260815_193207` 通过。注意：这是基于当前 rig 位姿的候选 odom，不等于 wheel-ground 真实动力学。
- 2026-08-15：完成阶段 14 第一版 Scout wheel-ground 真实动力学候选。新增 `VlnScoutWheelGroundController.cs`、`VlnFollowTransformPose.cs`、`VlnOffroadScoutWheelGroundCandidateProjectSetup.cs`、`VlnOffroadScoutWheelGroundCandidateSmokeTest.cs`、`VlnOffroadScoutWheelGroundCandidateSmokeTestRunner.cs` 和 `/home/ubuntu22/VLN/scripts/run_scout_wheel_ground_smoke_test.sh`；候选场景为 `Assets/VLN/Scenes/VLNOffroadScoutWheelGroundCandidate.unity`。验收 run id `vln_scout_wheel_ground_20260815_195417` 输出 `VLN_SCOUT_WHEEL_GROUND_SMOKE_TEST_PASS`，ROS2 图像、CameraInfo、PointCloud2、TF、`/vln/cmd_vel` 和 `/vln/odom` 均通过；物理根前进 `1.7739m`，前向位移 `1.771m`，并明确 `motion_source=wheel_ground_contact_not_kinematic_rig`。当前仍是候选级低速直行验证，下一步再做差速转向、坡地、碰撞和 `/joint_states`。
- 2026-08-15：阶段 14 后完成回归。旧 Scout URDF 控制场景 `run_scout_urdf_cmd_vel_smoke_test.sh` 已通过，run id `vln_scout_urdf_cmd_vel_20260815_195941`；完整资产基线 `run_asset_upgrade_baseline_check.sh` 已通过，run id `vln_asset_baseline_20260815_200044`，地图候选、Husky 视觉候选、标准输出、cmd_vel 控制和中文控制面板均未被 wheel-ground 候选破坏。
- 2026-08-16：新增用户手工运行清单 `/home/ubuntu22/VLN/user.md`，按终端拆分阶段 14 常用命令：清理 Unity lock、一键 wheel-ground 验收、启动 ROS-TCP-Endpoint、打开 Unity 工程、检查 topic、查看相机、查看 LiDAR/RViz、启动中文控制面板、直接发布 `/vln/cmd_vel`、停车、查看 `/vln/odom` 和退出后清理。
- 2026-08-16：根据用户反馈修正阶段 15 偏离需求的问题。上一版默认 9m 短路线没有覆盖桥、坡和终点方向，且速度过慢；长路线卡在前向约 `13.7m` 的桥面入口，根因是简化桥面碰撞体硬边形成物理台阶。已将默认路线升级为 54m 完整路线 `4,0;8,0;12,0;15,0;18,0;22,0;28,0;34,0;42,0;50,0;54,0`，提高速度参数，新增桥面前后物理过渡坡，修正 Scout 视觉朝向，轮胎视觉偏移提高到 `0.085m`。完整路线自动验收 `vln_scout_wheel_ground_route_20260816_150349` 通过：`reached_count=11/11`、`total_forward_progress=53.080m`、`stall_count=0`，图像、CameraInfo、点云和 odom 同时通过；基础 wheel-ground 回归 `vln_scout_wheel_ground_20260816_150809` 通过。
